# 🛠️ Установка базы данных PostgreSQL на Windows

Для работы сервера нужна база данных. Так как Docker не установлен, самый простой способ — установить PostgreSQL вручную.

## Шаг 1: Скачать и Установить

1. **Скачайте инсталлер:**
   👉 [PostgreSQL 16 for Windows (официальный сайт)](https://get.enterprisedb.com/postgresql/postgresql-16.2-1-windows-x64.exe)

2. **Запустите установку:**
   - Нажимайте **Next** во всех окнах.
   - ⚠️ **ОЧЕНЬ ВАЖНО:** Когда спросит пароль для superuser (`postgres`), введите:
     **`postgres`**
     *(Если введете другой — запомните его и поменяйте в файле `.env`)*
   - Порт оставьте **5432**.
   - Дождитесь окончания установки.
   - В конце снимите галочку "Stack Builder" (он нам не нужен), нажмите **Finish**.

## Шаг 2: Создать Базу Данных

1. Откройте **pgAdmin 4** (установился вместе с Postgres) или **SQL Shell (psql)** из меню Пуск.
2. Если открыли **SQL Shell (psql)**:
   - Server [localhost]: `Enter`
   - Database [postgres]: `Enter`
   - Port [5432]: `Enter`
   - Username [postgres]: `Enter`
   - Password for user postgres: `введите пароль (postgres)` (символы не отображаются!)
3. Введите команду для создания базы:
   ```sql
   CREATE DATABASE atfitk;
   ```
   *(Не забудьте точку с запятой в конце!)*
   Должен ответить: `CREATE DATABASE`.
4. Введите `\q` и `Enter`, чтобы выйти.

## Шаг 3: Проверка и Запуск

Теперь, когда база создана, вернитесь в VS Code и в терминале (в папке `backend`) выполните:

```bash
# 1. Запустить миграции (создать таблицы)
npm run migrate

# 2. Создать тестовых пользователей
npm run seed

# 3. Запустить сервер
npm run dev
```

Если всё прошло успешно, вы увидите: `✅ Migrations completed` и `🚀 ATFITK Backend API running`.

---

## ❓ Если пароль другой

Если вы при установке ввели другой пароль (не `postgres`), откройте файл `.env` в папке `backend` и измените:

```ini
DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/atfitk
```
